import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const azure = Color(0xFF007AFF);
  static const azureDeep = Color(0xFF005BBF);
  static const azureSoft = Color(0xFFE5F1FF);
  static const aqua = Color(0xFF32ADE6);
  static const mint = Color(0xFF34C759);
  static const amber = Color(0xFFFF9F0A);
  static const coral = Color(0xFFFF3B30);

  static const background = Color(0xFFF2F2F7);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceTint = Color(0xFFF7F7FA);
  static const border = Color(0xFFE5E5EA);
  static const borderStrong = Color(0xFFC7C7CC);
  static const ink = Color(0xFF1C1C1E);
  static const muted = Color(0xFF6E6E73);
}

class AppSpacing {
  const AppSpacing._();

  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 12.0;
  static const lg = 16.0;
  static const xl = 24.0;
  static const xxl = 32.0;
}

class AppRadii {
  const AppRadii._();

  static const card = 8.0;
  static const control = 8.0;
  static const pill = 999.0;
}
